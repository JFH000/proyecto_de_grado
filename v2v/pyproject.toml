[project]
name = "v2v"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pettingzoo>=1.27.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
